// next-sitemap.config.js
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://www.link.vacations/',
  generateRobotsTxt: true, // robots.txt bhi ban jayega
  sitemapSize: 5000,
}

export default config
